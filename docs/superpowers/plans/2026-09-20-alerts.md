# Alerts implementation plan

> Use superpowers:executing-plans inline under the user's existing continuous
> execution authorization. One independent whole-branch review remains required
> before release; no implementer delegation is needed.

**Goal:** Trader-created alerts with explicit timing, numeric/drawing/study
anchors, lifecycle, persistence and usable widget and OpenAlgo interfaces.

**Architecture:** Headless base controller, structural drawing provider, existing
PriceLine visuals and chart state/event bus; DOM stays in hosts and widget.

**Tech stack:** TypeScript, Vitest, Playwright, current package tiers. No new
runtime dependencies.

**Spec:** `../specs/2026-09-20-alerts-design.md`, implementing Part 2 of
`../specs/2026-09-20-open-interest-and-alerts-requirements.md`.

## Global constraints

- Keep original main checkouts and the user's eight OI edits untouched.
- No comparison brands, emojis or em/en dashes in additions.
- Preserve IndicatorAlertSpec and its existing indicator:alert behavior.
- No historical delivery on attach, reset, prepend, restore or replay.
- Default policy onBarClose; intrabar observations can later disappear.
- No automatic orders or delivery transport. Browser order routes are mocked.
- Measure bundle costs deliberately and update API/skills and final release facts.
- Chart persistence uses the existing restoreState API, not a new setState alias.

## Review focus

1. A trigger listener removes/disables another alert, throws or destroys the
   chart. State must already be consumed and later records must not fire stale.
2. History replacement preserves the tail time but changes its extrema. It is
   still history, not a new live observation.
3. Two identical studies restore with distinct anchors; a template replacement
   cannot silently retarget an alert to the first matching descriptor.
4. An idle market passes expiry or the wall clock moves backwards. No stale
   trigger or runaway timer is acceptable.
5. A line crosses a session gap or a log-price pane. Alert geometry must match
   the plotted shape, not a different raw-time interpolation.

## Task 1: Live price timing and lifecycle

Files: new `src/alerts/types.ts`, `src/alerts/conditions.ts`,
`src/alerts/controller.ts`; modify `src/core/chart.ts`, `src/index.ts`;
new `tests/alerts.test.ts`.

Interfaces: Chart.primaryBars(): readonly Bar[]; data:update payload
`{ kind: 'update' | 'reset' | 'prepend', time?: number }`.
AlertController(chart, { now? }) exposes add(input), update(id, patch), remove(id),
list(), enable(id), disable(id), setPaused(boolean), destroy(). Records carry
id/source/condition/policy/repeat/state/title/message/cooldownSeconds/expiresAt
and opaque payload. Numeric source thresholds are finite.

- [x] First write the erased-wick pair using a real measured Chart and source
  series, and verify it fails without the controller:
  ```ts
  series.setData([bar(60, 99), bar(120, 99)]);
  alerts.add({ source: { kind: 'price', price: 100 }, condition: 'crossingUp' });
  alerts.add({ source: { kind: 'price', price: 100 }, condition: 'crossingUp', policy: 'onTouch' });
  series.update({ ...bar(120, 99), high: 102 });
  // Only the onTouch record fired.
  series.update(bar(120, 99));
  series.update(bar(180, 99));
  // The bar-close record remains armed.
  ```
- [x] Add behavior cases for history silence, arm-time wick, live close crossing,
  each numeric condition, zero, invalid bounds, once/everyTime, cooldown, idle
  expiry, pause, context change, late history and event-listener reentrancy.
- [x] Implement the primary-source event after indicator invalidation, silent
  history reseeding, bounded per-record tail snapshots and one expiry timer.
  Set consumed state before emitting; stop evaluating removed/destroyed records.
- [x] Run `npx vitest run tests/alerts.test.ts` for red/green, then typecheck,
  affected chart/indicator/replay suites and the full library suite. Commit the
  verified price controller and exact API reference changes.

## Task 2: Indicator and named-condition sources

Files: `src/alerts/controller.ts`, `src/alerts/types.ts`, new
`src/alerts/bar-conditions.ts`; `src/model/indicator-registry.ts`,
`src/index.ts`; `tests/alerts-sources.test.ts`.

Interfaces: indicator source `{kind:'indicator', instanceId, plotKey, value,
upperValue?}`; bar source `{kind:'barCondition', id}` with condition matches.
Register named predicates by id, title and `when({bars,index})`; seed built-in
bullish/bearish/inside/outside/gap-up/gap-down conditions. A shared event base
carries alertId/title/message/time/index; trader payload adds alert and price.

- [x] Write real-chart tests with two EMA instances and a gapped plot, keeping
  thresholds in plot units. The second instance alone must trigger its alert.
  A missing plot must never act as zero. Predicates inspect only revealed bars.
- [x] Verify failures, resolve plot values only after flush through the public
  indicators accessor, expose unavailable reasons, and wire named predicates.
  Runtime custom predicate functions stay outside the persisted document.
- [x] Run source/controller suites and full library tests. Confirm the existing
  indicator-authored alert tests remain unchanged and passing. Commit.

## Task 3: Drawing anchors and visible lifecycle

Files: new `src/draw/alert-values.ts`; `src/draw/types.ts`, `tools.ts`,
`advanced-lines.ts`, `controller.ts`, `index.ts`; `src/alerts/controller.ts`;
new `tests/drawing-alerts.test.ts`, alert browser fixture/spec.

Interfaces: DrawingController.valueAt(id,time,level?) returns numeric bounds
and pane identity or undefined; availability and level choices expose reasons.
AlertController receives a structural drawing provider, never a draw-tier import.

- [x] Write geometry cases for finite trend segments, both ray directions,
  extended/horizontal lines, channels and configurable fib rungs. Cover a
  collapsed session gap and log scale using the real chart coordinate maps.
- [x] Write follow-drag and delete/undo/document-replacement regressions. Missing
  anchors emit removal; an unsupported tool reports why and draws no fake level.
- [x] Implement tool value hooks and provider wiring. Use PriceLine for fixed,
  drawing and indicator thresholds; styles and labels reflect all four states.
- [x] Run draw/controller suites, all three rendering projects and inspect the
  armed and triggered screenshots. Run full library checks and commit.

## Task 4: Versioned restoration and stable study anchors

Files: new `src/alerts/document.ts`; `src/model/chart-state.ts`,
`src/model/indicator-instance.ts`, `src/core/chart.ts`, `src/draw/controller.ts`,
`src/workspace/documents.ts`; new `tests/alerts-state.test.ts`.

Interfaces: AlertsDocument `{version:1, alerts: Alert[]}`; toJSON/fromJSON.
Chart alertState()/setAlertState(document); drawings:restore precedes
alerts:restore. IndicatorState.instanceId is optional for legacy documents.

- [x] Write round trips with repeated studies and drawing anchors; restore a
  legacy chart without alerts and migrate a bare alert list with policy omitted.
  Invalid versions/ids/bounds/payloads must reject without partial mutation.
- [x] Verify failures, preserve stable instance identity, and implement ordered
  restoration. Drop dangling anchors with a reason event after drawings restore.
  Restoring loaded history must produce no triggered events.
- [x] Validate chart state/workspace parsers and template behavior with real
  records. Run full tests/API/skills gates and commit.

Review follow-up: the user's 2026-09-20 handover in
`D:\OpenAlgo-Voice\codex instruction\openalgo-charts-remaining.md` confirms
the existing OI fold/timing choices and calls out the same restore ordering,
once-state retention and history-silence invariants. The response is in
`openalgo-charts-reply.md` beside that handover. Missing live OI remains absent;
capability and per-bar observation stay separate. Connected-broker validation
remains a test gate, not an alternate live OI algorithm. Preserve the existing
full aggregation rule in the final changelog and website release notes.

## Task 5: Widget editor, context actions and alert list

Files: new `src/alerts/schema.ts`, `src/widget/dialogs/alerts.ts`;
`src/widget/widget.ts`, `src/widget/dialogs/context-menu.ts`,
`src/widget/dialogs/index.ts`, widget styles and public entries;
new `tests/widget-alerts.test.ts` and browser cases.

Interfaces: alertSettingsSchema(source) supplies supported conditions and fields;
Widget.alerts is its owned AlertController. Pane/drawing context actions open the
editor with the clicked price or drawing id. Host delivery remains event-based.

- [x] Test create/edit, explicit timing copy, range fields, source/plot/level
  availability, once/everyTime/cooldown/expiry, enable/disable/delete and toasts.
- [x] Implement schema-generated controls using existing forms and panel
  lifecycles. Dispose listeners/timers and restore drawing state before alerts.
- [x] Run full widget tests and browser interactions in three engines; inspect
  desktop and narrow viewport pixels. Commit after size and import-boundary gates.

## Task 6: Hosts, documentation and complete validation

Files: reference host; isolated OpenAlgo terminal, chart UI and workspace paths;
README, ARCHITECTURE, CHANGELOG, API/skills and release documentation.

- [ ] Install the exact packed candidate in the isolated consumer. Add regressions
  for indicator/drawing alert creation, live triggering, replay silence, complete
  workspace restoration and disabled execution during all loading transitions.
- [ ] Implement host alert ownership and UI with event-based display. Exercise
  history/live/context changes and teardown without sending real orders.
- [ ] Document trigger definitions, erased-wick risk, missing data, idle expiry,
  scope and delivery ownership. Verify exports, versions and measured sizes.
- [ ] Run library/example/API/skills/build/size/shake, consumer full tests/build,
  and real Chromium/Firefox/WebKit harnesses with inspected screenshots. Record
  deterministic versus broker-connected evidence. Commit locally.
- [ ] Resume all unfinished F1-F9/P1-P6 and OI reference-secondary/release gates.
  The final release requires the independent whole-branch review, CI, npm,
  website and GitHub release process already authorized by the user.

## Pre-flight rulings

- User-provided spec plus explicit continuous implementation authorization
  supplies the design brief; routine interface choices are resolved here without
  another approval round. This preserves the user's requested execution method.
- Existing indicator alerts evaluate newly appended timestamps rather than an
  explicit close signal. Keep that compatibility behavior; the new controller
  implements onBarClose independently instead of claiming the old hook does it.
- A runtime optional document slot avoids importing the controller into Chart.
  Widget/hosts must attach it explicitly, just as they attach DrawingController.
- Task 1 produces the source event consumed by Tasks 2 and 3. Task 3 produces
  drawing resolution consumed by Task 4. Task 4 orders restoration consumed by
  Tasks 5 and 6. Tasks 2 and 4 preserve instance identity, not descriptor identity.

## Task 1 checkpoint

Implemented the structural headless price controller and primary-source update
bridge. Price alerts do not flush indicators or copy whole history per tick.
History/live distinction, arm-time snapshots, monotonic judged-bar watermarks,
context/replay/host pause, cooldown, one idle-expiry timer and reentrant teardown
are exercised by 26 real-chart tests. Source updates from secondary series do
not masquerade as primary updates, including when their global axis runs ahead.

Evidence: initial 19 tests RED without the controller; 19 GREEN after the first
implementation. Two history rollback regressions reproduced duplicate delivery,
then passed with monotonic watermarks. A lifecycle regression caught the wrong
creation event name and passed with the required alert:created event. Full suite
5409 tests in 224 files passed; example suite 235 tests in 18 files passed; lint,
typecheck, build and all 8 declaration checks passed. API generation with warnings
as errors and skill coverage 908/908 passed. Logs are under artifacts/candidate/
alerts-task1-*; RED logs also include alerts-controller-red, alerts-watermark-red
and alerts-events-red.

Ruling: the Chart event bus already swallows listener exceptions. Preserve this
contract and assert once-state remains consumed when delivery throws, rather
than changing event propagation. Cost if wrong: hosts must log their own delivery
failures, as before. Assertions inside these callbacks were moved outside the
bus so swallowed assertion errors cannot make a test pass vacuously.

Ruling: allow the measured feature cost in explicit bundle budgets. Base 79.00
to 80.69 kB, base plus trade 88.30 kB, widget terminal 187.75 kB, all tiers
226.23 kB. New limits are respectively 81, 88.5, 188 and 226.5 kB; unaffected
tier limits stay unchanged. The chart-only bridge grows 50.25 to 50.29 KiB,
with a 50.30 KiB cap and a new assertion that AlertController is absent. All
size/shake checks passed after recording these intentional allowances. Cost if
wrong: exported base consumers download about 1.69 kB more compressed code;
chart-only consumers pay about 0.04 KiB. Final release facts remain a later gate.

## Task 2 checkpoint

Added specific-instance plot thresholds and named candle conditions with an
immutable runtime descriptor registry. Missing plots/readings remain unavailable
and never become zero. The public availability method reports missing anchors,
context mismatch and pause; custom predicates receive only the prefix through
their evaluated bar. Trader and indicator-authored events share AlertEventPayload
without changing the existing indicator alert runtime.

The initial 14 source tests failed against Task 1. Two further RED regressions
caught a stale plot baseline after a consumed touch and a false crossing after
editing study settings. A final RED regression caught unnecessary study flushes
on every paused tick. All were fixed; the final source/controller coverage is
43 tests, and the full suite passed 5426 tests in 225 files. Existing indicator
alert suites remain unchanged and passing. Lint, typecheck, build, 8 declaration
checks, API with warnings as errors, coverage 912/912, and 235 reference-host
tests passed. Artifacts use alerts-task2-*, alerts-sources-* names.

Ruling: failed custom predicates emit alert:error once per evaluated bar and
leave other alerts running. Runtime predicates are not delivery hooks. Cost if
wrong: a predicate fixed during a bar waits until the next bar (or a controller
reload) before retrying. Gap-up/down mean the entire bar exceeds the previous
range, while inside/outside require strict extrema; these definitions are in
the public reference rather than left to host interpretation.

Ruling: allow 1.03 kB additional compressed base code for plot resolution,
availability, six built-ins and custom predicate registration. Measured base
81.72 kB, base plus trade 89.33 kB, widget terminal 188.78 kB and all tiers
227.26 kB, capped at 82, 89.5, 189 and 227.5 kB. The chart-only import remains
50.29 KiB, and explicit guards confirm both the controller and registry are
removed. Cost if wrong: hosts loading the full exported base pay that 1.03 kB;
chart-only imports have no additional cost for Task 2. Unchanged tier limits and
the 50.30 KiB chart-only cap remain enforced.

## Task 3 checkpoint

Added numeric drawing hooks and a structural provider for 13 supported tools:
trend line, ray, extended line, horizontal line/ray, trend angle, three channels
and four fib tools. Finite spans, extension direction, collapsed session gaps,
logarithmic and inverted panes follow the drawing's actual coordinate maps.
Channels expose explicit boundaries or a band; fib alerts require an active rung.
Other tools remain unavailable with a reason. Nonprice panes require an input
plot in the same pane. Drawing edits reseed evaluation, while delete, undo and
document replacement remove missing anchors with an event.

Fixed, plot and drawing levels reuse PriceLine with state labels and colors.
The private alert subclass paints above drawing strokes and never captures hits.
Pixel inspection initially found a drawing stroke crossing alert text; a new
pixel regression failed with 116 overlapping pixels and then passed with zero.
Native drawing drag and panning from an alert line remain covered in Chromium,
Firefox and WebKit. Armed and triggered screenshots were inspected in all three
engines; the harness uses synthetic data, not a connected broker.

Evidence: 30 new drawing/visual tests, 73 focused alert tests, 5456 total tests
in 226 files, and 235 reference-host tests passed. Six real-browser cases passed.
Lint, types, build, eight declaration checks, warning-free API generation and
skill coverage 912/912 passed. Logs are under artifacts/candidate/alerts-task3-*
and alerts-visual-overdraw-red.log.

Ruling: reuse a private PriceLine subclass rather than expanding the generic
primitive's public options. It changes only alert paint order and pointer hits.
Cost if wrong: alert visuals need their own layering adjustment, while existing
PriceLine consumers retain their current contract. Hosts own the drawing and
alert controllers together and destroy alerts before replacing their provider.

Ruling: allow the measured drawing resolution and visual lifecycle cost. Base
82.83 kB, base plus trade 90.44 kB, widget terminal 190.75 kB and all tiers
229.22 kB have limits of 83, 90.5, 191 and 229.5 kB. Draw remains 35.39 kB
under its existing 36 kB limit. Chart-only is 50.26 KiB, below the unchanged
50.30 KiB cap; controller and registry removal remain asserted. Cost if wrong:
full exported base consumers pay another 1.11 kB compressed, while chart-only
imports do not pay for alert geometry or visuals. Final release facts remain
a gate after the remaining alert and host work.

## Task 4 checkpoint

Added version-1 alert documents, detached serialization, legacy bare-list
migration and validation before mutation. Chart state carries alert documents
with or without an evaluator. The controller hydrates on attachment, persists
lifecycle changes before delivery callbacks and keeps closed/touched bar guards
through reload, including cooldown-suppressed touches. Runtime payload identity
is preserved until serialization, where unsupported JSON values reject explicitly.

Chart restore preserves specific study identities and reserves all restored IDs
before generating any missing legacy IDs. Reusable templates strip identity.
The drawing controller handles drawings:restore before alerts:restore validates
anchors; the widget's redundant manual drawing restores were removed. Missing
restored drawing/study/plot anchors emit removal reasons. Confirmed once alerts
remain triggered and visible; historical loading, attachment and restore stay
silent. Temporary inventory changes during restore cannot evaluate or reconcile
alerts prematurely.

Evidence: the initial persistence run failed 20 tests against the missing API.
Further RED regressions covered array accessors, explicit null settings, opaque
payload fields that workspace parsing would lose, and preservation of the
WorkspaceDocumentError contract. Thirty persistence tests now pass; the full
library suite passes 5486 tests in 227 files. Reference-host tests pass 235/235.
Nine alert browser cases pass across Chromium, Firefox and WebKit, including
an actual page reload with two triggered once alerts and all five visible levels.
Restored screenshots were inspected in each engine; no duplicate overlays or
deliveries appeared. Typecheck, lint, build, eight declaration checks, API with
warnings as errors, and skills coverage 913/913 pass. Logs use alerts-task4-*,
alerts-state-*, alerts-payload-* and alerts-workspace-error-* names.

Ruling: the core imports the shared alert document parser, not the controller.
This lets a chart without an evaluator validate and round-trip saved alerts
atomically. Cost if wrong: chart-only users pay the parser's compressed cost;
the evaluator, registry, drawing tier and widget still tree-shake away. Measured
chart-only 50.26 to 51.82 KiB, with a deliberate 51.90 KiB cap. Base is 84.14 kB,
base plus trade 91.75 kB, widget terminal 192.03 kB and aggregate 230.77 kB,
capped at 84.5, 92, 192.5 and 231 kB. Workspace is 5.28 kB under its existing
6 kB limit. Unaffected tier limits stay unchanged.

Ruling: portable workspace privacy rules still apply to opaque routing payloads,
but encountering a reserved field now rejects that payload instead of silently
stripping a field. Symbols, hidden properties and extra array properties also
reject rather than disappear. Cost if wrong: a host must keep private routing
information outside the portable document and resolve it from a nonprivate key.
Standalone alert documents remain opaque finite JSON without workspace-specific
account-field filtering.

Ruling: an absent alert document in a restored legacy chart clears the current
controller, while an absent drawing provider retains drawing alerts as unavailable.
An actual missing drawing in an attached provider drops its alert with a reason.
Cost if wrong: hosts that used partial chart restores must include alert state
when they intend to retain it. Template application must create fresh study
identities and explicitly retain any unrelated alerts it intends to keep.

## Task 5 checkpoint

The widget owns its alert controller and exposes an Alerts list on desktop and
through the mobile More sheet. The shared schema drives draft controls for
price, study plots, drawing levels and named candle conditions. Context menus
seed the actual price, drawing or study. Nonprice drawings require a compatible
plot, including a descriptor plot that overlays the price pane. Missing readings
stay unavailable and are never converted to zero.

Save validates drafts and UTC expiry; Cancel discards them. Context changes,
removed anchors and unsupported tools block stale drafts with an explanation.
Editing a triggered record preserves lifecycle state unless Enabled changes.
Numeric blanks survive blur for validation, existing expiry seconds survive a
rename, separate editors have unique label IDs, and selector changes retain
keyboard focus. Closed panels release their subscriptions.

Idle expiry updates the list and persistence. Cooldown-suppressed touches save
their consumed-bar guards. An opaque nonportable payload can fail explicit
serialization while automatic widget save reports the error and teardown still
completes. Widget alerts never send orders or external notifications.

Evidence: 21 widget alert regressions plus the overlay-pane regression were
observed failing before fixes. Full library suite: 5508 tests in 228 files.
Reference-host suite: 235 tests in 18 files. Eighteen alert browser cases pass
across Chromium, Firefox and WebKit, covering desktop, narrow screens, real
context-menu drawing creation, both timing policies, editing and actual reload.
Screenshots were inspected in all engines; a checked-state styling defect found
in pixels was reproduced in a browser regression and fixed. Types, lint, build,
eight declaration checks, API with warnings as errors, skills 916/916, size and
shake pass. The documentation website builds. Its existing lockfile-root and
framework-lint-plugin warnings remain, and browser runners report an inherited
color-environment warning. Logs use artifacts/candidate/alerts-widget-*.

Ruling: alerts:checkpoint is a persistence signal after silent history seeding
or consumed-bar guard changes. It does not request delivery. Lifecycle events
alone cannot save a cooldown-suppressed touch. Cost if wrong: custom hosts must
subscribe when they persist alerts outside the widget.

Ruling: the measured UI cost is accepted in the widget tier. Base 84.14 to
84.65 kB, widget 42.62 to 46.27 kB, terminal 192.03 to 196.19 kB, and aggregate
230.77 to 234.93 kB, Brotli. Limits are 85, 46.5, 196.5 and 235 kB; base plus
trade is 92.26 kB under 92.5. The packaging assertion now accepts decimal limits,
matching the existing size checker. Chart-only stays 51.82 KiB under 51.90, with
no alert evaluator, registry or widget in that import. Other tier limits remain.
Final release facts across all website/README surfaces remain a release gate.

The manual preview is tests/e2e/widget-alerts-fixture.html, with synthetic price
steps, a Close bar button and its own persisted namespace. It is not a connected
broker test or a reference-host migration. Task 6 and the broader production
plan remain open; version 2.4.5 is still unpublished.

## Task 6 host integration rulings

Ruling: expose createAlertUi in the widget tier for custom hosts. It reuses
the tested editor and list, while the host owns the chart, drawing controller,
alert controller, persistence and delivery. Its onOpenChange callback lets a
host suppress capture-phase trading shortcuts throughout nested dialogs.
Cost if wrong: custom hosts gain a small widget-tier dependency instead of
maintaining separate source selectors and lifecycle forms.

Ruling: chart rebuilds carry the complete chart document through the engine's
ordered restore. Do not separately replay drawings after alert restoration:
that removes anchors underneath restored alerts. Study identity belongs to
the document, rather than a newly generated host-side study ID.

Ruling: secondary chart state, request and width travel in the reference
layout document as an additive field. Restoration loads that chart's own
history before ordered state restoration. A cancelled or superseded load
cannot paint the replacement chart. Both charts mount their alert dialogs
over the workspace so narrow split panes do not clip source controls.

Ruling: the adapter's measured widget cost is 46.27 to 46.60 kB Brotli;
terminal 196.19 to 196.52 kB and aggregate 234.93 to 235.26 kB. Accept limits
46.75, 196.75 and 235.5 kB. The base and other tier limits remain unchanged.
Cost if wrong: full widget imports pay 0.33 kB for reusable custom-host UI.

## Task 6 reference-host checkpoint

The yfinance host now owns alert controllers and shared dialog adapters for
both charts. Toolbar Alerts follows the focused chart. Chart rebuilds retain
study IDs and drawing anchors; reload retains triggered once records without
historical delivery. Secondary chart state includes its request and width.
Alerts raise local notices only. History-only provider data carries no OI.

Replay selection and the asynchronous finer-history load pause evaluation.
Cancellation invalidates late work and aborts its dedicated request slot.
The finer-history cache now includes instrument and period, not only interval.
Order and bracket entry/repricing reject replay and unavailable-data states.
The second chart fences obsolete loads and does not paint after teardown.

Evidence: three adapter regressions failed before the adapter existed. The
duplicate drawing restore failed two persistence assertions. Browser checks
failed on the missing Alerts button, absent secondary restoration and the
171 px secondary editor on a phone before their fixes. Replay cache/cancel
and five execution-state regressions, plus bracket creation/repricing, were
observed failing before implementation. All 5511 library tests in 229 files
and 244 reference tests in 19 files pass. Twenty-eight reference browser
checks pass, including twelve alert cases across Chromium, Firefox and WebKit.
They cover price/study/drawing creation, preserved identities, reload, replay
load/cancel/playback silence, no order/fill side effects, and narrow dialogs.
Desktop, primary-narrow and secondary-narrow screenshots were inspected.

Types, lint, build, eight declaration checks, warnings-as-errors API generation,
skills 917/917, size/shake and the website build pass. Existing website root
and framework lint warnings remain; browser runners retain the inherited
color-environment warning. Logs use artifacts/candidate/alerts-reference-*.
The real-provider server is running on 8125: a Chromium probe loaded 251 AAPL
daily bars, opened the editor and recorded zero orders/fills and no page errors.
That is historical-provider evidence, not connected-broker validation.

Task 6 remains open for exact-packed OpenAlgo /trading integration and validation.
The consumer is still at bcd334203 with the prior OI candidate. The broader
F1-F9/P1-P6, connected-broker and 2.4.5 release gates remain unchanged.

## Task 6 consumer checkpoint

Consumer commit `834b582cc` installs the exact `2a7ca17` packed candidate.
All 33 installed package files match the archive. Version remains 2.4.0 for
development; the release target remains 2.4.5.

Each terminal now owns an alert controller and lazy shared editor/list.
Toolbar Alerts opens a viewport-wide dialog, including for narrow split panes
and fullscreen. Local notices consume both alert families' shared delivery
fields. The dialog blocks terminal execution and drawing shortcuts while open.
Rebuild and teardown release its UI, fullscreen listener, controller and host
subscriptions. This resource review is static plus lifecycle regressions;
it is not a sustained heap or connected-broker endurance measurement.

Saved study IDs survive rebuild and reload. Template append keeps existing
anchors but generates new identities for imported studies. Template replacement
retains drawings and unrelated alerts. Workspace preparation restores drawings
before validating alert anchors. History lookup/loading and failure pause
alerts and block order routes. Replay selection, asynchronous finer history,
playback and resume stay silent. Cancellation fences late replay activation.

Evidence: seven real terminal/engine alert tests, adapter identity regressions,
and a toolbar wiring regression were observed failing before implementation.
Full consumer suite passes 2271 tests in 141 files. Type checking and production
build pass. Consumer lint exits zero with two existing warnings and two infos;
the full suite reports the existing canvas stub notices, and Vite reports its
large chunk warning. Generated API coverage is 380/380, author API 64/64 and
capabilities 66/66. Consumer docs and skill prose describe the new contracts.

The packed consumer passes 43 combined checks per browser in Chromium, Firefox
and WebKit, 129 checks total. These include all source selectors, live trigger,
saved workspace reload, replay loading/cancellation, narrow dialog bounds,
OI, templates, history correctness and staged workspace failures. Desktop and
mobile alert screenshots were inspected. Alert checks add no order requests;
the existing trading checks use mocked transport. Logs and screenshots are in
`artifacts/candidate/alerts-consumer-*`. Harness JSON output uses `--output`;
the runs here retained logs and screenshots instead. Browser temp files were
moved to the D: artifact directory and heavy runs serialized after concurrent
bundler caches temporarily exhausted C: free space.

Ruling: browser readiness must wait for the active named grid, not its temporary
startup charts. Template dialog animation must finish before the next dialog,
and workspace checks must explicitly clear interval sync left by foundation
checks. These fixes retain the original assertions and make combined runs
exercise the intended chart generation.

Still open: consumer context-menu entry points, replay request abort ownership,
an explicit decision/test for alert lifecycle durability when a named workspace
has autosave off, additional fullscreen/theme/keyboard and teardown stress
checks, and read-only connected-broker validation. The current browser reload
case explicitly saves its workspace; it does not establish independent durable
alert history when workspace configuration is unsaved. Task 6 remains in
progress. All remaining F1-F9/P1-P6 and final release gates still apply.

### Consumer lifecycle persistence ruling

Keep a validated runtime snapshot per account/workspace/pane, independent of
layout autosave. Merge only state and duplicate guards onto saved alerts whose
ID, source, condition, timing, repetition, scope, cooldown, expiry and disabled
configuration still match. A renamed message can retain lifecycle; a changed
threshold cannot inherit an old trigger. Missing definitions are not recreated:
new alerts and source configuration remain part of explicit workspace Save or
Autosave. This preserves the workspace contract while preventing an already
saved once alert from rearming after reload merely because autosave was off.

Bind and restore the runtime snapshot after ordered chart restoration and before
the staged grid is unlocked. Failed/cancelled staging must not overwrite it.
Save-as seeds an independent namespace; workspace removal clears its snapshots.
Serialization uses the existing bounded alert document parser. Browser storage
failure is reported and does not interrupt teardown or other chart operations.
Tests must cover changed definitions, copied workspaces, account/pane isolation,
silent restoration, cancelled staging, quota failure and fired-once reload with
workspace autosave disabled.

### Context target identity ruling

The consumer browser check exposed that study legends identify their instance,
but plotted study lines are classified only by series type. Preserve the owning
instance and plot key in the public context target when a plotted series is hit.
Use painted series order, honor visibility and gaps, and retain drawing priority.
The widget and consumer editors must seed that exact plot. Legend targets keep
the existing first-plot fallback. Verify the actual pointer path, multiple plots,
duplicate instances, hidden plots and a drawing over a study before repacking.

The library plot-identity regression and widget plot/reading regression were
observed failing before the fix. The 131-test affected sweep and full verification
pass: 5513 library tests, 244 example tests, lint, types, build, declarations,
size and tree-shaking. API generation treats warnings as errors; skill coverage
remains 917/917. No size budget changed. This library checkpoint precedes the
new exact-package consumer browser run; Task 6 and release gates remain open.
