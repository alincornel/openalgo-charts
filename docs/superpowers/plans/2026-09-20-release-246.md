# Charts 2.4.6 release plan

> Use the review and verification skills. Root owns runtime corrections and release files; independent agents own read-only review, a dedicated browser regression and consumer preparation.

**Goal:** Validate PR 14 and release Charts 2.4.6, including its yfinance example, to npm/GitHub/Pages. Hand the consumer integration instructions to Claude; the user now excludes OpenAlgo changes.

**Architecture:** Preserve optional descriptor-driven source actions and marker anchoring. The engine emits source requests; the host owns source files and editors. Keep public defaults compatible and validate the actual built package in the consumer.

**Tech stack:** TypeScript, Vitest, Playwright, Rollup, Next website, React consumer, GitHub Actions trusted npm publishing.

**Spec:** User-approved PR 14 at 6a7d664 and the release process in CLAUDE.md.

## Constraints

- Preserve the original Charts OI edits, Claude's OpenAlgo branch/build and uncommitted OpenScript editor work.
- No comparison product names, emoji or long dashes in added code/docs/comments.
- No live orders, browser downloads or fixed viewport overrides in the signed-in browser.
- Do not move existing tags or republish 2.4.5. Use immutable 2.4.6 after successful checks.
- GitHub disallows approval by the PR author account; post substantive review evidence and merge only after required checks pass.

## Review focus

- Marker gaps must not substitute instrument prices onto an unrelated oscillator scale.
- Price anchoring must follow the displayed primary candle and release owned primitives on teardown.
- Icon size changes must preserve hit boxes, clipping and stacked legend spacing.
- Transparent color handling must not hide unrecognized but visible color values.
- Source actions must identify the correct instance without putting source storage into the engine.

## Tasks

- [x] Review the exact PR diff and tests; reproduce CI's 45-byte widget aggregate size overflow and any material defects before correction.
- [x] Run focused legend/marker regressions and add minimal regressions for confirmed review findings. Keep src/core/chart.ts under root ownership.
- [x] Add tests/e2e/indicator-source-markers.spec.ts and register it in the existing browser projects; inspect source action, transparency, icon sizing and price/gap markers in Chromium, Firefox and WebKit.
- [x] Update package.json, package-lock.json and src/version.ts to 2.4.6. Update CHANGELOG.md, README.md, ARCHITECTURE.md, diagram facts, website current-version/release/indicator/example pages and .github/skills references using measured counts and sizes.
- [x] Run npm run verify, npm run skills:coverage, warning-free API generation and website build. Measure final tier sizes and registry counts; make every current factual reference agree without changing historical release notes.
- [x] User-added scope: demonstrate source access, candle-anchored signals and chart-local legend sizing in the yfinance reference host, including both panes, rebuild/persistence behavior and browser coverage.
- [x] Validate the original packed candidate in the isolated consumer: 566 trading tests, types/build, index/coverage and 186 mocked browser checks. Further OpenAlgo work was explicitly removed from scope. Validate the final archive in a neutral package-install folder and hand integration to Claude.
- [ ] Post PR review, push release amendments to PR 14, wait for green CI and merge. Preserve unrelated OpenScript PR 2084 scope.
- [ ] Tag v2.4.6 and dispatch the Release workflow. Verify npm archive bytes, version, integrity, signature/provenance and create the GitHub release from the final changelog. Wait for Pages and inspect changed deployed examples/assets.
- [ ] Hand published package details and indicator-index regeneration instructions to Claude. Do not install, commit or push consumer changes; the user reserved OpenAlgo integration for Claude.
- [ ] Record exact commits, checks, release links and any remaining host-integration limits in this plan and a concise handover file.

## Initial findings

PR 14's CI passes unit tests and declarations, then fails the widget aggregate
size cap by 45 bytes: 205.04 kB versus 205 kB. The base is 91.47 kB against 91.5.
The PR already records a measured chart-only tree-shake increase. Final budgets
will be reviewed against measured feature cost rather than silently disabled.

The original OpenAlgo checkout is on feat/openscript-studies at b3845becd, with
uncommitted generated frontend assets. Its source-button integration is in open
PR 2084. Preserve that work while selecting the minimal consumer pin delivery.

## Reviewed candidate evidence

The original 24 focused tests passed while actual chart reproductions failed.
New regressions reproduced NaN gap overwrites, incorrect left-axis placement,
instrument prices substituted into oscillators, stale primary bindings, mixed
legend row heights and CSS transparent readings. A browser pixel check also
found one label repainting another label's tail because its path was retained.
These defects are corrected. Independent rereview found no remaining material
defect in those paths. Marker fallback resolves the current primary and scale;
unrelated scales and oscillator gaps remain absent.

Full npm run verify passed: 5771 engine tests in 245 files, 389 reference tests
in 30 files, seven endurance-harness checks, lint, types, build, declarations,
size and tree-shaking. Skill coverage is 930/930 and indicator benchmarks pass.
The new browser regression passes all 12 checks across Chromium, Firefox and
WebKit; screenshots were inspected for colors, geometry and source controls.
The complete 441-case browser sweep and website checks are separate gates.

Final measured decimal Brotli sizes: base 91.57 kB, base + trade 99.58 kB,
indicators 29.84 kB, draw 35.43 kB, transform 4.50 kB, profile 14.96 kB,
webgl 6.39 kB, widget 48.30 kB, workspace 5.52 kB, terminal 205.15 kB,
all tiers 244.53 kB. The chart-only import is 52.75 KiB. The base, terminal
and chart-only budgets are 92 kB, 205.5 kB and 53 KiB; the measured feature
cost and prior release baseline are recorded in the bundling reference.

The final publish candidate contains 33 files. Its only difference from the
consumer validation archive is the updated README; all runtime bundles,
declarations, maps and package metadata match byte for byte. Publication and
consumer registry acceptance remain pending until the later gates complete.

The original candidate subsequently passed all 441 browser cases and the
warning-free API generation and website build. Both website demo routes passed
source opening, anchor changes, icon sizing, themes and narrow viewports in
Chromium, Firefox and WebKit, without page errors or failed assets. Before
merging, the user added the yfinance reference example to the release scope.
That host update and its checks must complete before publication. The temporary
forms/alignment redesign request was explicitly canceled and is outside scope.

The user subsequently instructed that OpenAlgo must not be changed because
Claude owns /trading. Consumer editing and remaining checks were stopped.
The active OpenAlgo checkout was untouched. Preparation remains uncommitted in
the separate openalgo-production worktree on chore/charts-246-pin, with a
temporary local-tarball dependency that must not be merged as-is. Before the
scope change, 566 trading tests, types/build, generated-index and coverage
checks passed; Chromium passed all 62 mocked acceptance checks including an
actual source click and below-candle marker placement. No consumer commit or
push is part of this release. The final handover records the integration steps.

## Final reference-host candidate

The yfinance host now registers an opt-in Source signal sample before restoring
studies. It resolves source requests against the emitting chart's instance and
known descriptor, writes source using textContent, and closes/unsubscribes when
the owner is removed or destroyed. Legend size is chart-local, supports preview
and Cancel, and survives rebuilds, legacy snapshots and named workspaces. A fresh
read-only review found no material defect in those paths. Full npm run verify
passes again with 5771 engine tests, 401 reference tests in 31 files and seven
harness checks; runtime bundle measurements are unchanged.

The isolated consumer's Firefox and WebKit checks had completed before cleanup,
so its final archived result is 62 checks per browser, 186 total. The user scope
restriction remains in force: no more OpenAlgo changes, installs or commits.

The focused yfinance browser checks passed in all three engines. Its first
broader sweep passed 162 cases and timed out during one existing test's page
initialization while Rollup was also rebuilding dist. That failed case and the
reference sweep must be resolved with stable bundles before merge; no assertion
or product behavior is weakened to accommodate the timeout.

The isolated initialization retry and a clean reference sweep passed against
stable bundles: 166 browser checks, including 12 source, marker, legend and
narrow-dialog cases across the three engines. The timeout did not reproduce;
its exact cause remains unconfirmed.

## Incoming dialog changes

Claude pushed 99e8df2 to PR 14 while the yfinance commit 64b2d0b was being
validated. The branches were merged without overwriting either contribution.
The incoming commit adds shared form columns, host metric tokens, correct
select-chevron positioning and chart-zone expiry fields. This is review of
Claude's incoming implementation, not a separate visual redesign.

Review reproduced an unchanged expiry moving when the chart timezone changed,
browser-dependent default dates, month-end overflow, a lost translated label
and help control, and a compile break for existing typed translation catalogs.
Regression fixes and browser verification cover those findings. The incoming
commit's CI failed only on measured size budgets, so final measurements and
documented budget adjustments are required before publication.

## Combined release candidate

Full verification passes with 5804 engine tests in 246 files, 401 reference-host
tests in 31 files, seven harness checks, lint/types/build/declarations/size and
tree-shaking. Skills coverage is 930/930, runtime audit reports no vulnerabilities,
API generation has no warnings, and the website build succeeds. All 21 alert
browser cases pass across Chromium, Firefox and WebKit; screenshots verify host
metrics, shared columns, internal chevrons, narrow fields and pinned expiry zones.
The full combined 462-case browser sweep is a remaining merge gate.

Final decimal Brotli sizes are 91.55 kB base, 99.56 kB base plus trade,
49.00 kB widget, 205.83 kB terminal and 245.21 kB all tiers. Other measured
tier sizes are unchanged. The chart-only import remains 52.75 KiB. Widget,
terminal and total limits are 49.25, 206 and 245.5 kB respectively, with the
feature cost recorded in the bundling reference.

The final 33-file archive installs in a neutral package folder and passes all
nine entry-point imports, shared error identity, version and registry checks
(105 indicators, 15 registered chart types and 85 drawing tools). It supersedes
the earlier consumer archive; no additional OpenAlgo installation was performed.
